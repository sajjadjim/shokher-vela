specVersion: "v1"
serviceId: "hobby-hub-client"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hobby-hub-client-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
